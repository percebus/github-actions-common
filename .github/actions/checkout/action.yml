name: checkout
description: git checkout and do some other things
inputs:
  # The SSH key is configured with the local
  # git config, which enables your scripts to run authenticated git commands. The
  # post-job step removes the SSH key.
  #
  # We recommend using a service account with the least permissions necessary.
  #
  # [Learn more about creating and using encrypted secrets](https://help.github.com/en/actions/automating-your-workflow-with-github-actions/creating-and-using-encrypted-secrets)
  ssh-private-key:
    description: SSH key used to fetch the repository.
    required: false

  repository:
    description: Repository name with owner. For example, actions/checkout
    required: false
    default: ${{ github.repository }}

  # When checking out the repository that triggered a workflow, this defaults to the reference or SHA for that event.
  # Otherwise, uses the default branch.
  ref:
    description: The branch, tag or SHA to checkout.
    required: false
    default: ${{ github.sha }}

  # Number of commits to fetch.
  # 0 indicates all history for all branches and tags.
  fetch-depth:
    description: git checkout fetch-depth
    required: false
    default: "1"

  # Whether to checkout submodules: `true` to checkout submodules or `recursive` to
  # recursively checkout submodules.
  #
  # When the `ssh-key` input is not provided,
  # SSH URLs beginning with `git@github.com:` are converted to HTTPS.
  #
  # Default: false
  submodules:
    description: Whether to update submodules
    required: false
    default: "false"

  path:
    description: Relative path under $GITHUB_WORKSPACE to place the repository
    required: false
    default: ""

  ls_options:
    description: ls options
    required: false
    # in windows, la does NOT work
    default: "-la"

runs:
  using: composite
  steps:
    # SRC: https://github.com/actions/checkout/releases/tag/v7.0.1
    - name: checkout ${{ inputs.repository }}
      uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      with:
        ssh-key: ${{ inputs.ssh-private-key }}
        repository: ${{ inputs.repository }}
        ref: ${{ inputs.ref }}
        # submodules: ${{ inputs.submodules }}  # TODO?
        fetch-depth: ${{ inputs.fetch-depth }}
        path: ${{ inputs.path }}

    # XXX? Does `checkout.submodules` do this?
    - name: git submodule update
      if: ${{ inputs.submodules == 'true' }}
      shell: bash
      run: |
        git submodule update --init --recursive
        git submodule update --remote

    - name: ls
      shell: bash
      run: ls ${{ inputs.ls_options }}
